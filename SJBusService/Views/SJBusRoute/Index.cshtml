@model IEnumerable<SJBusService.Models.busRoute>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.busRouteCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.routeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.busRouteCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.routeName)
        </td>
        <td>
            @Html.ActionLink("Monitorted Trips", "../SJTrip/Index", new { id=item.busRouteCode  }) |
            @Html.ActionLink("Edit", "Edit", new { id=item.busRouteCode  }) |
            @Html.ActionLink("Details", "Details", new {id=item.busRouteCode  }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.busRouteCode  }) |
            @Html.ActionLink("Schedule", "Index", "SJRouteSchedule" ,new { id=item.busRouteCode },null) |
            @Html.ActionLink("Stops", "Index", "SJRouteStop" ,new { id=item.busRouteCode },null)

        </td>
    </tr>
}

</table>
