@model SJBusService.Models.busStop

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>busStop</legend>

        @Html.HiddenFor(model => model.busStopNumber)

        <div class="editor-label">
            @Html.LabelFor(model => model.location)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.location)
            @Html.ValidationMessageFor(model => model.location)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.busStopNumber)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.busStopNumber)
            @Html.ValidationMessageFor(model => model.busStopNumber)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.goingDowntown)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.goingDowntown)
            @Html.ValidationMessageFor(model => model.goingDowntown)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
